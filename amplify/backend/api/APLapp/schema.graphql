type TeamMember @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstName: String!
  lastName: String!
  email: AWSEmail!
  teamsID: ID! @index(name: "byTeams")
  jerseyNumber: Int!
  phoneNumber: String!
  address: AWSJSON
  teamMemberType: String
}

type Teams @model @auth(rules: [{allow: public}]) {
  id: ID!
  teamName: String
  Forms: [TeamMember] @hasMany(indexName: "byTeams", fields: ["id"])
}
 